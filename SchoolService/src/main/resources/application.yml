server:
  port: 8503
spring:
  boot:
    admin:
      client:
        url: http://localhost:1111
  application:
    name: school-service
  cloud:
    consul:
      discovery:
        register: true
        service-name: school-service
      host: localhost
      port: 8500
  
   