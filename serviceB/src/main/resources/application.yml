server:
  port: 8082
spring:
  application:
    name: service-B
  cloud:
    consul:
      discovery:
        register: true
        service-name: service-B
      host: localhost
      port: 8500   datasource:    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql/localhost/feign
    username: root
    password: Tata123! 
       